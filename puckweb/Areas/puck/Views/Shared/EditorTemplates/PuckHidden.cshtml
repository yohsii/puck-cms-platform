@model Object

@{
    var value = "";
    if(Model!=null){
        if (Model is DateTime){
            value = ((DateTime)Model).ToString("dd/MM/yyyy hh:mm:ss");
        }else{
            value = Model.ToString();
        }
    }
}
@Html.Hidden("",value)